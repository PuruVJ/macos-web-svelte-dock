import {
  increment
} from "./chunk-N3PVPDBY.js";
import {
  effect_root,
  effect_tracking,
  get,
  render_effect,
  source,
  tick
} from "./chunk-OZEFIYIP.js";
import {
  derived,
  get as get2,
  readable,
  readonly,
  writable
} from "./chunk-2PT4R7PF.js";
import "./chunk-MA2GOT3I.js";
import {
  noop
} from "./chunk-47C36IZF.js";
import "./chunk-UGBVNEQM.js";

// ../../node_modules/.pnpm/svelte@5.0.0-next.244/node_modules/svelte/src/store/index-client.js
function toStore(get3, set) {
  const store = writable(get3(), (set2) => {
    let ran = false;
    const teardown = effect_root(() => {
      render_effect(() => {
        const value = get3();
        if (ran) set2(value);
      });
    });
    ran = true;
    return teardown;
  });
  if (set) {
    return {
      set,
      update: (fn) => set(fn(get3())),
      subscribe: store.subscribe
    };
  }
  return {
    subscribe: store.subscribe
  };
}
function fromStore(store) {
  let value = (
    /** @type {V} */
    void 0
  );
  let version = source(0);
  let subscribers = 0;
  let unsubscribe = noop;
  function current() {
    if (effect_tracking()) {
      get(version);
      render_effect(() => {
        if (subscribers === 0) {
          let ran = false;
          unsubscribe = store.subscribe((v) => {
            value = v;
            if (ran) increment(version);
          });
          ran = true;
        }
        subscribers += 1;
        return () => {
          subscribers -= 1;
          tick().then(() => {
            if (subscribers === 0) {
              unsubscribe();
            }
          });
        };
      });
      return value;
    }
    return get2(store);
  }
  if ("set" in store) {
    return {
      get current() {
        return current();
      },
      set current(v) {
        store.set(v);
      }
    };
  }
  return {
    get current() {
      return current();
    }
  };
}
export {
  derived,
  fromStore,
  get2 as get,
  readable,
  readonly,
  toStore,
  writable
};
//# sourceMappingURL=svelte_store.js.map
